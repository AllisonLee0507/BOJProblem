#define _CRT_SECURE_NO_WARNINGS
#if 0
#include <iostream>
using namespace std;
int main(){
    int n, b, i;
    scanf("%d%d", &n, &b);
    int p[n], s[n], x[n];
    int l;
    l = 0;
    for(i = 0; i <n; i++){
        scanf("%d%d", &p[i], &s[i]);
        x[i] = p[i] + s[i];
    }
    
    
}
#endif
