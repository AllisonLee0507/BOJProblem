#define _CRT_SECURE_NO_WARNINGS
#if 0
#include <stdio.h>
#include <math.h>
int main(){
    int N;
    scanf("%d", &N);
    int X;
    X = pow(2, N);
    X = pow(X+1, 2);
    printf("%d", X);
}
#endif

#if 0
#include <iostream>
#include <cmath>
using namespace std;
int main(){
    int N;
    cin >> N;
    int X;
    X = pow(2, N);
    X = pow(X+1, 2);
    cout << X;
}
#endif
